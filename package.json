{
  "name": "spidercam",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @spidercam/server",
    "start": "npm run start -w @spidercam/server",
    "check": "npm run lint && npm run format && npm run build && npm run typecheck --workspaces --if-present && npm run test",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --check",
    "format:fix": "oxfmt .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "npm run test -w @spidercam/shared && npm run test:unit -w @spidercam/server",
    "test:integration": "npm run test:integration -w @spidercam/server",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "oxfmt": "^0.14.0",
    "oxlint": "^1.69.0"
  },
  "engines": {
    "node": ">=20"
  }
}
