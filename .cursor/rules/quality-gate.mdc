---
description: Quality gate reminders for agents working in spidercam
alwaysApply: true
---

# Quality Gate

After implementation, run:

- `npm run lint`
- `npm run format`
- `npm run check`
- a Fallow audit through MCP when the change is structural, deletion-heavy, or otherwise risky

When finding a bug, change into cursor debug mode.
